# Utilise une image nginx légère pour servir les fichiers statiques
FROM nginx:alpine

# Copie les fichiers du frontend dans le répertoire par défaut de nginx
COPY . /usr/share/nginx/html

# Expose le port 80 (port interne de nginx)
EXPOSE 80

# Lance nginx en mode daemon off (nécessaire pour que le container reste actif)
CMD ["nginx", "-g", "daemon off;"]